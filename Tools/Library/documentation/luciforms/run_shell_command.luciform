<🜲luciform_doc id="run_shell_command">

  <!-- Le Pacte : Ce que fait l'outil -->
  <🜄pacte>
    <type>execution</type>
    <intent>Exécute une commande shell et retourne son résultat.</intent>
    <level>fondamental</level>
  </🜄pacte>

  <!-- L'Invocation : Comment utiliser l'outil -->
  <🜂invocation>
    <signature>run_shell_command(command: str) -> dict</signature>
    <requires>
      <param>command</param>
    </requires>
    <returns>dict: Contient stdout, stderr, return_code, et success.</returns>
  </🜂invocation>

  <!-- L'Essence : Le sens profond de l'outil -->
  <🜁essence>
    <keywords>
      <keyword>shell</keyword>
      <keyword>command</keyword>
      <keyword>execute</keyword>
      <keyword>run</keyword>
    </keywords>
    <symbolic_layer>Projette une commande dans le néant du shell et en capture l'écho. C'est un pacte puissant et dangereux. À utiliser avec une intention claire.</symbolic_layer>
    <usage_context>Quand il est nécessaire d'interagir directement avec le système d'exploitation sous-jacent, par exemple pour lister des fichiers, installer des dépendances, ou exécuter des scripts.</usage_context>
  </🜁essence>

</🜲luciform_doc>
