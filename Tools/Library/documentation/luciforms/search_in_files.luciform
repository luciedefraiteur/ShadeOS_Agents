<🜲luciform_doc id="search_in_files">

  <!-- Le Pacte : Ce que fait l'outil -->
  <🜄pacte>
    <type>search</type>
    <intent>Scanne le contenu de multiples fichiers à la recherche d'une séquence de symboles (regex).</intent>
    <level>intermédiaire</level>
  </🜄pacte>

  <!-- L'Invocation : Comment utiliser l'outil -->
  <🜂invocation>
    <signature>search_in_files(pattern: str, search_path: str = ".") -> list[dict]</signature>
    <requires>
      <param>pattern</param>
    </requires>
    <optional>
      <param>search_path</param>
    </optional>
    <returns>list[dict]: Une liste de dictionnaires, chaque dictionnaire contenant file_path, line_number, et line_content.</returns>
  </🜂invocation>

  <!-- L'Essence : Le sens profond de l'outil -->
  <🜁essence>
    <keywords>
      <keyword>search</keyword>
      <keyword>regex</keyword>
      <keyword>files</keyword>
      <keyword>content</keyword>
    </keywords>
    <symbolic_layer>Déploie un réseau de clairvoyance sur les grimoires, révélant les motifs cachés dans leurs murmures.</symbolic_layer>
    <usage_context>Quand il est nécessaire de trouver des occurrences spécifiques de texte ou de code à travers un ou plusieurs fichiers, par exemple pour localiser toutes les utilisations d'une fonction ou d'une variable.</usage_context>
  </🜁essence>

</🜲luciform_doc>
